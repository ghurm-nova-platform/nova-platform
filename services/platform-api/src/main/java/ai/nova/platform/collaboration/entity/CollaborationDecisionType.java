package ai.nova.platform.collaboration.entity;

public enum CollaborationDecisionType {
    APPROVE,
    REJECT,
    RESOLVE_CONFLICT,
    PAUSE,
    RESUME,
    CANCEL,
    REQUEST_REVIEW,
    REQUEST_APPROVAL,
    REQUEST_CLARIFICATION
}
