package ai.nova.platform.identity.ad;

import org.springframework.stereotype.Service;

@Service
public class ActiveDirectorySynchronizationService {

    public void synchronize(String domain) {
        // AD sync stub.
    }
}
