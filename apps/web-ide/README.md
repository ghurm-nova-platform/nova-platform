# Nova Web IDE

Browser-based development workspace built around Monaco Editor, terminal sessions, project files, Git operations, AI chat, preview, and controlled agent actions.

## Security rule

The browser never receives infrastructure credentials or unrestricted host access. Commands execute through isolated sandbox workers and permission gates.

## Ownership

Developer Experience Team
