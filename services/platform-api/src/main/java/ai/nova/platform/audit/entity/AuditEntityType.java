package ai.nova.platform.audit.entity;

public enum AuditEntityType {
    RELEASE,
    DEPLOYMENT,
    ROLLBACK,
    ENVIRONMENT,
    POLICY,
    MERGE,
    APPROVAL,
    TASK,
    REPOSITORY,
    USER,
    CONFIGURATION
}
