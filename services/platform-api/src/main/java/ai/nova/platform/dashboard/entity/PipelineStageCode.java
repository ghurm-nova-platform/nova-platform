package ai.nova.platform.dashboard.entity;

public enum PipelineStageCode {
    PLANNER,
    CODING,
    REVIEW,
    TESTING,
    PATCH,
    GIT,
    PULL_REQUEST,
    CI,
    REPAIR,
    APPROVAL_GATE,
    MERGE,
    RELEASE,
    DEPLOYMENT,
    ROLLBACK
}
